import { createClient } from "@/lib/supabase/server";
export default async function MessagesPage() { const supabase = await createClient(); const { data } = await supabase!.from("contact_messages").select("*").order("created_at", { ascending: false }); return <div><h2 className="mb-6 font-serif text-2xl">Contact messages</h2><div className="space-y-4">{(data ?? []).map((item) => <article key={item.id} className="rounded-panel border border-border p-5"><div className="flex justify-between"><strong>{item.name}</strong><span className="text-sm text-muted">{item.email}</span></div><p className="mt-3 whitespace-pre-wrap text-sm text-muted">{item.message}</p></article>)}</div></div>; }
