import { createClient } from "@/lib/supabase/server";
export default async function CustomersPage() { const supabase = await createClient(); const { data } = await supabase!.from("profiles").select("*").order("created_at", { ascending: false }); return <div><h2 className="mb-6 font-serif text-2xl">Customers</h2><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr className="border-b border-border text-muted"><th className="py-3">Name</th><th>Email</th><th>Phone</th><th>Role</th></tr></thead><tbody>{(data ?? []).map((item) => <tr key={item.id} className="border-b border-border"><td className="py-4">{item.full_name}</td><td>{item.email}</td><td>{item.phone}</td><td className="capitalize">{item.role}</td></tr>)}</tbody></table></div></div>; }
